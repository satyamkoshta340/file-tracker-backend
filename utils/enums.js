const FileStatus = {
    created: "created",
    sent: "sent",
    recieved: "recieved"
}

const ResponseStatus = {
    success: "success",
    failure: "fail"
}

module.exports = {
    FileStatus,
    ResponseStatus
}